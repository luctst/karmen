// Tailwind v4 uses the dedicated PostCSS plugin. Apps that consume @karmen/ui
// can re-export this (`@karmen/ui/postcss.config`) or define their own.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}
